import{_ as s,C as l,o as t,c as h,a2 as a,b as d,w as o,a as n,E as r,a3 as c}from"./chunks/framework.CZW044RH.js";const _=JSON.parse('{"title":"How it works","description":"","frontmatter":{},"headers":[],"relativePath":"how-it-works.md","filePath":"how-it-works.md"}'),p={name:"how-it-works.md"};function w(u,e,b,g,m,v){const i=l("Mermaid");return t(),h("div",null,[e[2]||(e[2]=a('<h1 id="how-it-works" tabindex="-1">How it works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How it works&quot;">​</a></h1><p>sugr has two technical problems to solve, and everything else builds on top: <strong>calling native C functions from Java without JNI</strong>, and <strong>compiling the whole thing to a small standalone binary</strong>.</p><h2 id="ffm-instead-of-jni" tabindex="-1">FFM instead of JNI <a class="header-anchor" href="#ffm-instead-of-jni" aria-label="Permalink to &quot;FFM instead of JNI&quot;">​</a></h2><p>sugr&#39;s <code>core</code> module binds directly to the OS&#39;s native webview library (<code>libwebview</code> - itself a thin wrapper over WebView2/WKWebView/WebKitGTK) using the <a href="https://openjdk.org/jeps/454" target="_blank" rel="noreferrer">Foreign Function &amp; Memory API</a> (<code>java.lang.foreign</code>), not JNI. No C glue code to compile per platform - <code>Linker.downcallHandle()</code> binds directly to exported C functions (<code>webview_create</code>, <code>webview_navigate</code>, <code>webview_bind</code>, ...), and <code>Linker.upcallStub()</code> lets native code call back into Java (used for the <code>invoke</code>/<code>emit</code> bindings, and for <code>sqlite3_exec</code>&#39;s row callback in the <code>examples/sql-client</code> app).</p><h2 id="application-lifecycle" tabindex="-1">Application lifecycle <a class="header-anchor" href="#application-lifecycle" aria-label="Permalink to &quot;Application lifecycle&quot;">​</a></h2><p><code>Application.builder()</code> only collects configuration - <code>title()</code>, <code>size()</code>, <code>frontend()</code>, <code>bind()</code>/<code>bindAsync()</code>, <code>on()</code>, <code>onReady()</code> all just populate a <code>Builder</code>. Nothing native happens until <code>builder.run()</code>:</p><ol><li><strong>Load the native lib.</strong> Extract the bundled <code>webview.dll</code>/<code>.so</code>/<code>.dylib</code> from the classpath to a temp file, bind its exported functions via FFM (<code>NativeLibrary</code>).</li><li><strong>Create the window.</strong> <code>webview_create</code> returns a handle; a <code>UiDispatcher</code> is built around it (from this point, sugr knows which thread is &quot;the UI thread&quot; for the rest of the run).</li><li><strong>Configure it.</strong> <code>webview_set_title</code>, <code>webview_set_size</code>.</li><li><strong>Inject the events bootstrap.</strong> <code>webview_init</code> runs a small script (<code>window.__sugrEvents__</code>, an event emitter) before <em>any</em> page loads, including the very first navigation.</li><li><strong>Bind the two fixed upcalls.</strong> <code>&quot;invoke&quot;</code> (JS calls into Java - see <a href="#the-bridge-protocol">The bridge protocol</a> below) and <code>&quot;emit&quot;</code> (JS→Java events). This is the <em>entire</em> JS↔Java surface - every <code>@Bind</code> method and every hand-written <code>bind()</code> call goes through the same <code>&quot;invoke&quot;</code> binding.</li><li><strong>Navigate.</strong> To the dev server URL (<code>sugr dev</code>, real HMR) or to the embedded <code>AssetServer</code>&#39;s local base URL (<code>sugr build</code>&#39;s production path) - see <a href="/guide/window">Window &amp; frontend</a>.</li><li><strong>Fire <code>onReady(this)</code>.</strong> The one point where your code gets the <code>Application</code> handle before the event loop starts - stash it if background code needs to call <code>emit()</code> later.</li><li><strong>Block on the event loop.</strong> <code>webview_run</code> blocks the calling thread until the window closes. This is why <code>run()</code> must be called from your app&#39;s main thread and nowhere else.</li></ol>',7)),(t(),d(c,null,{default:o(()=>[r(i,{id:"mermaid-60",class:"mermaid",graph:"flowchart%20TD%0A%20%20%20%20A%5B%22Builder%3A%20title%2C%20size%2C%20frontend%2C%20bind%2C%20bindAsync%2C%20on%2C%20onReady%22%5D%20--%3E%7Cbuilder.run%7C%20B%0A%20%20%20%20B%5B%221.%20load%20webview.dll%20via%20FFM%22%5D%20--%3E%20C%5B%222.%20webview_create%20returns%20handle%2C%20plus%20a%20new%20UiDispatcher%22%5D%0A%20%20%20%20C%20--%3E%20D%5B%223.%20webview_set_title%2C%20webview_set_size%22%5D%0A%20%20%20%20D%20--%3E%20E%5B%224.%20webview_init%20runs%20the%20events%20bootstrap%20JS%22%5D%0A%20%20%20%20E%20--%3E%20F%5B%225.%20webview_bind%20invoke%2C%20and%20webview_bind%20emit%22%5D%0A%20%20%20%20F%20--%3E%20G%5B%226.%20webview_navigate%20to%20the%20target%20url%22%5D%0A%20%20%20%20G%20--%3E%20H%5B%227.%20onReady%20callback%20fires%22%5D%0A%20%20%20%20H%20--%3E%20I%5B%228.%20webview_run%20blocks%20the%20calling%20thread%22%5D%0A%20%20%20%20I%20-.-%3E%7CJS%2FJava%20traffic%20via%20the%20invoke%20and%20emit%20upcalls%2C%20until%20the%20window%20closes%7C%20I%0A%20%20%20%20I%20--%3E%20J%5B%22webview_destroy%22%5D%0A%20%20%20%20J%20--%3E%20K%5B%22assetServer.stop%2C%20embedded%20mode%20only%22%5D%0A"})]),fallback:o(()=>[...e[0]||(e[0]=[n(" Loading... ",-1)])]),_:1})),e[3]||(e[3]=a('<p>While the loop runs, everything is event-driven, not sequential - see <a href="#the-bridge-protocol">The bridge protocol</a> for the <code>invoke</code>/<code>emit</code> upcalls and <a href="#thread-model">Thread model</a> for how replies/events hop back onto the UI thread from other threads.</p><p><strong>Shutdown:</strong> <code>webview_run</code> returns once the user closes the window → <code>webview_destroy</code> → the embedded <code>AssetServer</code> (if any) is stopped. There&#39;s currently no app-level &quot;before close&quot; hook - closing the window ends <code>run()</code> with no further extension point.</p><h2 id="the-bridge-protocol" tabindex="-1">The bridge protocol <a class="header-anchor" href="#the-bridge-protocol" aria-label="Permalink to &quot;The bridge protocol&quot;">​</a></h2><p>JS never calls arbitrary native functions - there&#39;s exactly one entry point, <code>window.invoke(method, paramsJson)</code>, injected by a single <code>webview_bind</code> call in <code>Application.run()</code>. Every <code>@Bind</code>-generated or hand-written binding goes through it:</p>',4)),(t(),d(c,null,{default:o(()=>[r(i,{id:"mermaid-73",class:"mermaid",graph:"sequenceDiagram%0A%20%20%20%20participant%20JS%20as%20JS%20(%40sugr%2Fruntime)%0A%20%20%20%20participant%20Raw%20as%20window.invoke%20(webview%20glue)%0A%20%20%20%20participant%20App%20as%20Application%20(core)%0A%20%20%20%20participant%20Bridge%20as%20Bridge%20(bridge%20module)%0A%0A%20%20%20%20JS-%3E%3ERaw%3A%20invoke(%22hello%22%2C%20%5B%22world%22%5D)%0A%20%20%20%20Raw-%3E%3EApp%3A%20native%20req%3A%20%5B%22hello%22%2C%20%22%5B%5C%22world%5C%22%5D%22%5D%0A%20%20%20%20App-%3E%3EBridge%3A%20Bridge.dispatch(reqJson)%0A%20%20%20%20Bridge-%3E%3EBridge%3A%20look%20up%20%22hello%22%20handler%2C%20call%20it%0A%20%20%20%20Bridge--%3E%3EApp%3A%20JSON%20result%20string%0A%20%20%20%20App-%3E%3ERaw%3A%20webview_return(handle%2C%20seq%2C%20isError%2C%20resultJson)%0A%20%20%20%20Raw--%3E%3EJS%3A%20Promise%20resolves%2Frejects%0A"})]),fallback:o(()=>[...e[1]||(e[1]=[n(" Loading... ",-1)])]),_:1})),e[4]||(e[4]=a('<p><code>Bridge</code> doesn&#39;t know about webview at all - <code>Application</code> (in <code>core</code>) owns the FFM calls and the JSON-RPC-shaped envelope; <code>Bridge</code> just routes a method name to a handler and returns JSON text. This is why hand-written <code>bind()</code> calls and <code>@Bind</code>-generated ones can freely mix: they&#39;re both just entries in the same <code>Bridge</code> handler map.</p><p>Events (<code>events.emit()</code>/<code>events.on()</code>) are a second, parallel channel - <code>webview_bind(&quot;emit&quot;, ...)</code> for JS→Java, and a JS-side <code>__sugrEvents__</code> dispatcher (injected once via <code>webview_init</code>, before any page loads) for Java→JS, driven by <code>Application.emit()</code> calling <code>webview_eval</code>.</p><h2 id="thread-model" tabindex="-1">Thread model <a class="header-anchor" href="#thread-model" aria-label="Permalink to &quot;Thread model&quot;">​</a></h2><p><code>webview_create</code> + <code>webview_run</code> must run on the same thread (the OS&#39;s UI thread - on macOS specifically the <em>main</em> thread). Any native call from a different thread has to go through <code>webview_dispatch</code>, which schedules a callback back onto the UI thread. <code>Application.emit()</code> always does this, which is why it&#39;s safe to call from anywhere. Bind/invoke replies go through the same <code>UiDispatcher</code> path (<code>Application.reply()</code>), which runs inline if already on the UI thread or hops via <code>webview_dispatch</code> otherwise - this works reliably regardless of which thread completes the bind&#39;s <code>CompletableFuture</code>, since the webview request id is copied to a Java <code>String</code> synchronously before any async work starts (see <code>reply()</code>&#39;s javadoc for why that copy matters).</p><h2 id="no-reflection-at-the-bind-boundary" tabindex="-1">No reflection at the bind boundary <a class="header-anchor" href="#no-reflection-at-the-bind-boundary" aria-label="Permalink to &quot;No reflection at the bind boundary&quot;">​</a></h2><p><code>@Bind</code> methods are dispatched via a <strong>generated</strong> switch-shaped class - the <code>processor</code> module reads your method&#39;s signature at compile time (via <code>javax.annotation.processing</code>, not runtime reflection) and emits direct calls (<code>target.hello(arg0)</code>), plus JSON encode/decode calls into <code>bridge.Json</code> for whatever the parameter/return types need (records, <code>List</code>, <code>Map</code>, <code>CompletableFuture</code>). Nothing about the bind path needs a <code>reachability-metadata.json</code> entry for native-image - the FFM downcalls in <code>core</code> still do (that&#39;s inherent to FFM, not something <code>@Bind</code> can remove), but you generate that once with <code>native-image-agent</code> and it doesn&#39;t grow as you add more <code>@Bind</code> methods.</p><h2 id="native-image" tabindex="-1">Native-image <a class="header-anchor" href="#native-image" aria-label="Permalink to &quot;Native-image&quot;">​</a></h2><p>GraalVM&#39;s <code>native-image</code> does closed-world, whole-program static analysis and compiles straight to a native binary - no JVM at runtime, no JIT warm-up, much lower baseline memory than running the same app on a JVM (this is also <em>why</em> native-image builds take longer than a <code>javac</code> compile and why <code>sugr dev</code> intentionally runs on a plain JVM rather than rebuilding a native image on every save - see <a href="/guide/build">Building &amp; packaging</a>).</p>',8))])}const y=s(p,[["render",w]]);export{_ as __pageData,y as default};
