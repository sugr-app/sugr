import{_ as s,o as t,c as i,a2 as a}from"./chunks/framework.DhRvvru9.js";const u=JSON.parse('{"title":"Global shortcuts","description":"","frontmatter":{},"headers":[],"relativePath":"guide/shortcuts.md","filePath":"guide/shortcuts.md"}'),o={name:"guide/shortcuts.md"};function n(d,e,l,c,r,h){return t(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="global-shortcuts" tabindex="-1">Global shortcuts <a class="header-anchor" href="#global-shortcuts" aria-label="Permalink to &quot;Global shortcuts&quot;">​</a></h1><p><code>core.GlobalShortcut</code> registers a system-wide keyboard shortcut that fires a callback even when the app&#39;s window isn&#39;t focused - useful for things like a quick-capture hotkey on a background/tray-resident app. Like <a href="/sugr/guide/native"><code>core.Dialogs</code>/<code>core.Clipboard</code>/<code>core.Notification</code></a>, it&#39;s a plain Java class - not wired into <code>Application.Builder</code> or exposed to JS automatically. You call it from your own code, and bridge to JS yourself (e.g. via <code>@Emits</code>) if you want the frontend to react to it:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> com.sugr.core.GlobalShortcut;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">AutoCloseable handle </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GlobalShortcut.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">register</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Ctrl+Shift+K&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Notification.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">show</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sugr&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Quick capture triggered&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// later, to unregister just this one shortcut:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">handle.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">close</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><h2 id="api" tabindex="-1">API <a class="header-anchor" href="#api" aria-label="Permalink to &quot;API&quot;">​</a></h2><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">GlobalShortcut.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">register</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(String accelerator, Runnable action)  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// -&gt; an AutoCloseable handle, or null on non-Windows</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">GlobalShortcut.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">closeAll</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()                                       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// -&gt; kills the shared background listener entirely</span></span></code></pre></div><p><code>accelerator</code> is modifiers joined by <code>+</code>, ending in a single key - e.g. <code>&quot;Ctrl+Shift+K&quot;</code>, <code>&quot;Alt+F4&quot;</code>, <code>&quot;Ctrl+Space&quot;</code>. Supported modifiers: <code>Ctrl</code>/ <code>Control</code>, <code>Alt</code>, <code>Shift</code>, <code>Win</code>/<code>Meta</code>/<code>Cmd</code> (case-insensitive). The key can be a single letter/digit, <code>F1</code>-<code>F24</code>, or one of a small set of named keys: <code>Space</code>, <code>Enter</code>/<code>Return</code>, <code>Escape</code>/<code>Esc</code>, <code>Tab</code>, <code>Backspace</code>, <code>Delete</code>, and the arrow keys (<code>Left</code>/<code>Up</code>/<code>Right</code>/<code>Down</code>).</p><p>Closing the handle returned by <code>register</code> unregisters just that one shortcut - it does <strong>not</strong> shut down the shared background listener, since other shortcuts may still be registered against it. Call <code>closeAll()</code> (e.g. from a shutdown hook) to tear the whole thing down explicitly.</p><h2 id="how-it-s-implemented" tabindex="-1">How it&#39;s implemented <a class="header-anchor" href="#how-it-s-implemented" aria-label="Permalink to &quot;How it&#39;s implemented&quot;">​</a></h2><p>Windows only for now - <code>register</code> returns <code>null</code> on macOS/Linux. Real system-wide global hotkeys need accessibility permissions (macOS) or X11-specific APIs (Linux) that don&#39;t fit this project&#39;s shell-out pattern, so rather than ship unverified code for them, they&#39;re left as a <code>null</code> return until there&#39;s a verified approach.</p><p>On Windows, the first call to <code>register</code> lazily spawns one persistent hidden PowerShell process shared by every subsequent registration in the JVM - the same &quot;long-lived hidden process&quot; shape <a href="/sugr/guide/window"><code>core.Tray</code></a> uses for its <code>NotifyIcon</code>, generalized to support registrations added and removed at any time rather than built once upfront:</p><ul><li>The process hosts a hidden <code>System.Windows.Forms.Form</code> subclass (via <code>Add-Type</code>) whose <code>WndProc</code> catches <code>WM_HOTKEY</code> and prints <code>&quot;HOTKEY:&lt;id&gt;&quot;</code> to stdout - a virtual thread on the Java side reads those lines and dispatches to the matching <code>Runnable</code>.</li><li>Each <code>register</code>/handle-<code>close()</code> call writes a <code>&quot;REGISTER:&lt;id&gt;:&lt;mods&gt;:&lt;vk&gt;&quot;</code> / <code>&quot;UNREGISTER:&lt;id&gt;&quot;</code> line to the process&#39;s stdin; a background thread inside the script reads those commands and marshals the actual <code>RegisterHotKey</code>/<code>UnregisterHotKey</code> Win32 calls onto the form&#39;s own thread (required since <code>RegisterHotKey</code> must be called from the thread that owns the window handle).</li></ul><div class="warning custom-block"><p class="custom-block-title">Verified on Windows only</p><p>The Windows implementation has been exercised end to end in a real running app. macOS and Linux have no implementation yet - <code>register</code> simply returns <code>null</code> there.</p></div>`,12)])])}const k=s(o,[["render",n]]);export{u as __pageData,k as default};
